CREATE USER 'fyp_user'@'localhost' IDENTIFIED BY 'fyp_password@my123!';
GRANT ALL PRIVILEGES ON * . * TO 'fyp_user'@'localhost';
FLUSH PRIVILEGES;
CREATE DATABASE fyp;


CREATE TABLE car_data (
    id int NOT NULL AUTO_INCREMENT,
    car_reg varchar(255),
    date varchar(255),
    valid_permit varchar(255),
    x_coord varchar(255),
    y_coord varchar(255),
    PRIMARY KEY (ID)
);


CREATE TABLE valid_permits (
    id int NOT NULL AUTO_INCREMENT,
    car_reg varchar(255),
    paidDate varchar(255),
    PRIMARY KEY (ID)
);


insert into valid_permits(car_reg, paidDate) values('12CW1484', '18-10.2018');
insert into car_data(car_reg) values('123');

